[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spirulae-splat"
version = "2026.4.1"

[project.scripts]
spirulae-train = "spirulae_splat.ss_trainer:entrypoint"
